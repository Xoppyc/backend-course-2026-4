import http from 'http';
import { Router } from '../router/Router';
interface ServerOptions {
    port?: number;
    host?: string;
    cors?: string | string[];
}
export declare class Server {
    private router;
    private options;
    constructor(router: Router, options?: ServerOptions);
    listen(callback?: () => void): http.Server;
}
export declare function createApp(options?: ServerOptions): {
    router: Router;
    listen: (callback?: () => void) => http.Server<typeof http.IncomingMessage, typeof http.ServerResponse>;
};
export {};
//# sourceMappingURL=Server.d.ts.map